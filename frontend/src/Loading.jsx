/**
 * Loading component is used to display a loading message while data is being fetched.
 *
 * @returns JSX element representing a loading message.
 */
export default function Loading() {
    return (
        <p>Loading...</p>
    )
}